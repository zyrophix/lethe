linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - ineffassign
    - unused
    - misspell
    - gofmt

run:
  timeout: 5m

issues:
  exclude-rules:
    - linters:
        - errcheck
      source: "defer .*Close\\(\\)"